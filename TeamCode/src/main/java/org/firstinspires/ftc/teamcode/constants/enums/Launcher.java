package org.firstinspires.ftc.teamcode.constants.enums;

public enum Launcher {
    LEFT(0),
    CENTER(1),
    RIGHT(2);

    public final int index;

    Launcher(int index) {
        this.index = index;
    }
}
